{
  "name": "report-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "汇报项目集合：纯前端 HTML 报告，托管于 Cloudflare Pages",
  "scripts": {
    "new": "node scripts/new-report.mjs",
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy . --project-name=report-cli"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
